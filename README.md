# 🏛️ نظام إدارة المنافسات والمشتريات - جامعة شقراء

## 📋 نظرة عامة
نظام متكامل لإدارة:
- 🏆 المنافسات
- 🛡️ الضمانات
- 📄 العقود
- 💰 الصرف

---

## 🚀 التشغيل للمرة الأولى

### 1. تثبيت المكتبات:
```bash
cd MUN
npm install
```

### 2. إعداد قاعدة البيانات:

انسخ ملف `.env.example` إلى `.env`:
```bash
copy .env.example .env
```

ثم عدّل `.env` وأضف:
- `DATABASE_URL` من المشروع الرئيسي (نفس القيمة)
- `NEXTAUTH_SECRET` (مفتاح سري طويل)
- `NEXTAUTH_URL="http://localhost:3001"`

### 3. تطبيق قاعدة البيانات:
```bash
npx prisma generate
npx prisma db push
```

### 4. إنشاء مستخدم إداري أولي:
```bash
node scripts/create-admin.js
```

### 5. تشغيل المشروع:
```bash
npm run dev
```

المشروع سيعمل على: **http://localhost:3001**

---

## 📁 البنية

```
MUN/
├── app/                    # صفحات التطبيق
│   ├── login/             # تسجيل الدخول
│   ├── dashboard/         # الصفحة الرئيسية
│   ├── competitions/      # المنافسات
│   ├── warranties/        # الضمانات
│   ├── contracts/         # العقود
│   ├── expenses/          # الصرف
│   └── admin/             # لوحة التحكم
├── lib/                   # المكتبات المساعدة
├── prisma/                # قاعدة البيانات
└── public/                # الملفات العامة
```

---

## 🎨 التصميم

مستوحى من جامعة شقراء مع لمسات عصرية:
- 🟢 الأخضر الرئيسي
- ⚪ الأبيض
- 🟡 الذهبي

---

## 🔗 التكامل مع المشروع الرئيسي

النظام مصمم ليعكس في قسم العمل في الموقع الرئيسي تلقائياً.

---

## 📝 الحالة الحالية

### ✅ تم إنجازه:
- إعداد Next.js + TypeScript + Tailwind
- تصميم قاعدة البيانات (4 أقسام)
- نظام تسجيل الدخول
- صفحة تسجيل دخول عصرية

### 🔄 قيد التطوير:
- صفحة Dashboard
- صفحات الأقسام الأربعة
- لوحة التحكم
- النسخ الاحتياطي
- التكامل مع الموقع الرئيسي
