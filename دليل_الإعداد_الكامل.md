# 🚀 دليل الإعداد الكامل - نظام MUN

## 📋 نظرة عامة

**نظام إدارة المنافسات والمشتريات - جامعة شقراء**

### الأقسام الأربعة:
- 🏆 المنافسات
- 🛡️ الضمانات
- 📄 العقود  
- 💰 الصرف

---

## ⚡ الإعداد السريع (5 دقائق)

### 1. تثبيت المكتبات

```bash
cd c:\Users\hp\Desktop\موقعي\MUN
npm install
```

⏱️ **المدة المتوقعة**: 3-5 دقائق

---

### 2. إعداد ملف .env

أنشئ ملف `.env` في مجلد MUN وأضف:

```env
# قاعدة البيانات (نفس القيمة من المشروع الرئيسي)
DATABASE_URL="postgresql://user:password@host:port/database"

# NextAuth
NEXTAUTH_SECRET="مفتاح-سري-طويل-جداً-يجب-ان-يكون-عشوائي"
NEXTAUTH_URL="http://localhost:3001"

# Admin Account (اختياري)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin123"
```

**📝 ملاحظة**: انسخ `DATABASE_URL` من:
```
c:\Users\hp\Desktop\موقعي\survivor\.env
```

---

### 3. إعداد قاعدة البيانات

```bash
npx prisma generate
npx prisma db push
```

سيتم إنشاء **9 جداول جديدة**:
- MunUser
- MunPermission
- Competition
- Warranty
- Contract
- ContractPayment
- Expense
- MunActivity
- MunBackup

---

### 4. إنشاء مستخدم إداري

```bash
node scripts/create-admin.js
```

**بيانات الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

⚠️ **مهم**: غيّر كلمة المرور بعد أول دخول!

---

### 5. تشغيل المشروع

```bash
npm run dev
```

🌐 **الموقع سيعمل على**: http://localhost:3001

---

## 🎨 التصميم

### الألوان المستخدمة:
- 🟢 **الأخضر**: اللون الرئيسي (جامعة شقراء)
- ⚪ **الأبيض**: الخلفيات
- 🟡 **الذهبي**: التفاصيل
- 🔵 **الأزرق**: العقود
- 🟣 **البنفسجي**: الصرف
- 🟠 **البرتقالي**: المنافسات
- 🟢 **الأخضر**: الضمانات

### الخطوط:
- **Cairo**: الخط الرئيسي (من Google Fonts)

---

## 🔐 نظام الصلاحيات

### أنواع الصلاحيات:

لكل قسم (المنافسات، الضمانات، العقود، الصرف):

1. **view** - عرض: يسمح بالدخول ورؤية البيانات
2. **edit** - تعديل: يسمح بالإضافة والتعديل والحذف
3. **admin** - إدارة: صلاحيات كاملة + النسخ الاحتياطي

### المدير (Admin):
- صلاحية `isAdmin = true`
- صلاحيات كاملة على جميع الأقسام
- الوصول إلى لوحة التحكم

---

## 📂 البنية

```
MUN/
├── app/
│   ├── login/              # تسجيل الدخول
│   ├── dashboard/          # الصفحة الرئيسية
│   ├── competitions/       # 🏆 المنافسات
│   ├── warranties/         # 🛡️ الضمانات
│   ├── contracts/          # 📄 العقود
│   ├── expenses/           # 💰 الصرف
│   ├── admin/              # ⚙️ لوحة التحكم
│   │   ├── users/          # إدارة المستخدمين
│   │   ├── permissions/    # الصلاحيات
│   │   ├── backup/         # النسخ الاحتياطي
│   │   └── ...
│   └── api/
│       └── auth/           # NextAuth API
├── components/
│   └── Header.tsx          # الهيدر الرئيسي
├── lib/
│   ├── prisma.ts           # Prisma Client
│   ├── auth.ts             # NextAuth Config
│   └── access.ts           # التحقق من الصلاحيات
├── prisma/
│   └── schema.prisma       # قاعدة البيانات
└── scripts/
    └── create-admin.js     # إنشاء مستخدم إداري
```

---

## 🔧 الأوامر المفيدة

### التطوير:
```bash
npm run dev          # تشغيل المشروع (port 3001)
```

### قاعدة البيانات:
```bash
npx prisma generate  # تحديث Prisma Client
npx prisma db push   # تطبيق التغييرات على قاعدة البيانات
npx prisma studio    # فتح واجهة قاعدة البيانات
```

### الإنتاج:
```bash
npm run build        # بناء المشروع
npm start            # تشغيل المشروع المبني
```

---

## 🌐 النشر على Vercel

### 1. ربط المشروع:
```bash
cd c:\Users\hp\Desktop\موقعي\MUN
vercel
```

### 2. تحديد الإعدادات:
- Project Name: `mun-system`
- Framework: Next.js
- Root Directory: `./`

### 3. إضافة المتغيرات البيئية:
في Vercel Dashboard:
- `DATABASE_URL`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL` (استبدل بالرابط الفعلي)

### 4. النشر:
```bash
vercel --prod
```

---

## 🔗 التكامل مع الموقع الرئيسي

سيتم عكس النظام في قسم العمل في الموقع الرئيسي تلقائياً.

### الرابط في الموقع الرئيسي:
```
/sections/work → سيظهر زر للدخول إلى نظام MUN
```

---

## 📝 ملاحظات مهمة

1. **المشروع منفصل تماماً** عن المشروع الرئيسي
2. **يستخدم نفس قاعدة البيانات** (جداول منفصلة)
3. **البورت 3001** (لن يتعارض مع المشروع الرئيسي على 3000)
4. **تصميم مستوحى من جامعة شقراء** مع لمسات عصرية

---

## 🆘 حل المشاكل

### المشكلة: npm install فشل
**الحل**: تأكد من الاتصال بالإنترنت وأن Node.js مثبت

### المشكلة: خطأ في قاعدة البيانات
**الحل**: تأكد من أن `DATABASE_URL` صحيح في `.env`

### المشكلة: لا يمكن تسجيل الدخول
**الحل**: أعد تشغيل `node scripts/create-admin.js`

---

## ✅ جاهز للاستخدام!

الآن يمكنك:
- ✅ تسجيل الدخول بحساب admin
- ✅ إضافة مستخدمين جدد
- ✅ إدارة الصلاحيات
- ✅ البدء بإضافة البيانات في الأقسام الأربعة

---

**🎉 مبروك! النظام جاهز للعمل**
